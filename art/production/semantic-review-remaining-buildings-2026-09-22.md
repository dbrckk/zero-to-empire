# Remaining building semantic review — 2026-09-22

Scope: BLD-04, BLD-05, BLD-07, BLD-08, BLD-10 (35 sprites, T0–T6).

Strict rule: technical QA alone is insufficient. No runtime promotion without semantic review. No text/logo, site-card/platform/ground decoration, detached unrelated structures, or family identity drift.

## BLD-04 — REJECT T0–T6
Reviewed the isolated T0–T6 sheet. Family identity and tier progression are strong, but the candidate still contains broad opaque ground/shadow masses and late tiers add landscaping/trees. This repeats the semantic class recorded in the queue (platform/landscaping) and is not eligible for runtime promotion.

Required retry: building only; alpha outside silhouette; no ground plane, landscaping, trees, cast-shadow card, labels or detached scenery.

## BLD-05 — REJECT T0–T6
Progression is readable, but multiple tiers retain large rectangular/irregular ground-shadow cards and detached side structures. The family therefore fails the connected isolated-building contract.

Required retry: preserve the blue/gold energy-hub DNA while removing ground cards and detached satellites. One connected primary silhouette, transparent outside it, no text.

## BLD-07 — REJECT T0–T6
The orange industrial family is visually coherent, but the reviewed sheet contains persistent ground/platform wedges, blue floor/shadow patches in later tiers and detached side fragments. This matches the prior BLD-07 platform/site-card rejection class.

Required retry: one connected shadowless blank-facade building; no slab/site card, no detached fragments, no pseudo-text.

## BLD-08 — REJECT T0–T6
Run 35660093773 is technically coherent and keeps the central Dyson hub, but the rendered sprites contain blueprint-style measurement annotations. T5/T6 also contain visible DYSON/Dyson text. Text/annotation is a hard semantic failure.

Required retry: keep the same central-hub/ring/collector progression, but render only the object on true transparency: no blueprint measurements, captions, labels, text or presentation board.

## BLD-10 — APPROVE T0–T6 FOR RUNTIME PROMOTION
The cleaned portal-only family passes semantic review:
- one persistent circular gateway identity across T0–T6;
- clear structural progression without identity replacement;
- no text, labels, logos, site card, landscaping or detached unrelated scenery;
- isolated transparent presentation;
- stable blue/white/gold material language;
- late tiers increase architectural complexity while preserving the portal as the dominant object.

Decision: **APPROVE T0–T6 for runtime promotion**. Runtime conversion/integration and final runtime QA are still required before strict DONE.

## Result
- Approved for runtime promotion: 7 / 35 (BLD-10)
- Rejected for regeneration: 28 / 35 (BLD-04/05/07/08)
- Strict DONE is intentionally unchanged until approved masters are integrated and runtime QA passes.
