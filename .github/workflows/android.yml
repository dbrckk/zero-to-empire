name: Android CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

concurrency:
  group: android-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'

      - name: Unit tests, release lint, debug, APK and app bundle builds
        run: gradle --no-daemon testDebugUnitTest lintRelease assembleDebug assembleRelease bundleRelease

      - name: Upload release lint report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ZeroToEmpire-release-lint
          path: app/build/reports/lint-results-release.html
          if-no-files-found: warn
          retention-days: 14

      - name: Upload release obfuscation mapping
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: ZeroToEmpire-release-mapping
          path: app/build/outputs/mapping/release/mapping.txt
          if-no-files-found: error
          retention-days: 90

      - name: Upload release app bundle
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: ZeroToEmpire-release-aab
          path: app/build/outputs/bundle/release/app-release.aab
          if-no-files-found: error
          retention-days: 30

      - name: Upload debug APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: ZeroToEmpire-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 14
